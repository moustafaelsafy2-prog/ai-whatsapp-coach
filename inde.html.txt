<!DOCTYPE html><html lang="ar" dir="rtl"><head>    <meta charset="UTF-8">    <meta name="viewport" content="width=device-width, initial-scale=1.0">    <title>الكبسولة الصحية - بث مباشر</title>    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">    <style>        :root {            --bg-dark: #0a0f16;            --bg-gradient-1: #0d1b2a;            --bg-gradient-2: #1b263b;            --primary-glow: #00ffcc;            --secondary-glow: #ff007f;            --text-main: #ffffff;            --text-muted: #b0bec5;            --glass-bg: rgba(255, 255, 255, 0.05);            --glass-border: rgba(255, 255, 255, 0.1);        }        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Cairo', sans-serif; }        body {            background-color: #030712;            color: var(--text-main);            overflow: hidden;            width: 100vw;            height: 100vh;            display: flex;            justify-content: center;            align-items: center;        }        .tiktok-container {            position: relative;            width: 450px;            height: 800px;            background: linear-gradient(135deg, var(--bg-gradient-1) 0%, var(--bg-gradient-2) 100%);            overflow: hidden;            box-shadow: 0 0 100px rgba(0, 0, 0, 0.9);            display: flex;            flex-direction: column;            border-radius: 20px;            border: 1px solid rgba(255, 255, 255, 0.05);            z-index: 50;        }        .bg-orb {            position: absolute;            border-radius: 50%;            filter: blur(80px);            z-index: 0;            opacity: 0.4;            animation: floatOrb 15s infinite alternate ease-in-out;            pointer-events: none;        }        .orb-1 { width: 300px; height: 300px; background: var(--primary-glow); top: -10%; left: -10%; }        .orb-2 { width: 250px; height: 250px; background: var(--secondary-glow); bottom: -10%; right: -10%; animation-delay: -5s; }        @keyframes floatOrb {            0% { transform: translate(0, 0) scale(1); }            100% { transform: translate(30px, -50px) scale(1.2); }        }        .app-content {            position: absolute;            top: 60px;            width: 100%;            display: flex;            flex-direction: column;            align-items: center;            padding: 20px;            z-index: 10;        }        .main-header h1 {            font-weight: 900;            font-size: 2.2rem;            background: linear-gradient(to right, #fff, var(--primary-glow));            -webkit-background-clip: text;            -webkit-text-fill-color: transparent;            text-align: center;            margin-bottom: 5px;        }        .capsule-wrapper {            position: relative;            width: 140px;            height: 280px;            margin-top: 40px;            transition: all 1s cubic-bezier(0.68, -0.55, 0.27, 1.55);        }        .capsule-top {            position: absolute;            width: 100%;            height: 50%;            border-radius: 70px 70px 0 0;            background: linear-gradient(135deg, #ff3366, #ff0044);            box-shadow: inset -5px -5px 15px rgba(0,0,0,0.3);            transition: transform 1s ease;            z-index: 2;        }        .capsule-bottom {            position: absolute;            width: 100%;            height: 50%;            top: 50%;            border-radius: 0 0 70px 70px;            background: linear-gradient(135deg, #ffffff, #e0e0e0);            box-shadow: inset -5px 5px 15px rgba(0,0,0,0.1);            transition: transform 1s ease;            z-index: 2;        }        .capsule-core {            position: absolute;            top: 50%;            left: 50%;            transform: translate(-50%, -50%) scale(0.5);            width: 360px;            background: rgba(13, 27, 42, 0.95);            backdrop-filter: blur(20px);            border-radius: 25px;            padding: 25px;            text-align: center;            opacity: 0;            visibility: hidden;            transition: all 0.6s ease;            border: 1px solid var(--primary-glow);            box-shadow: 0 0 40px rgba(0, 255, 204, 0.2);            z-index: 1;        }        .open .capsule-top { transform: translateY(-160px); opacity: 0; }        .open .capsule-bottom { transform: translateY(160px); opacity: 0; }        .open .capsule-core { transform: translate(-50%, -50%) scale(1); opacity: 1; visibility: visible; }        .timer-ring {            width: 65px;            height: 65px;            border-radius: 50%;            background: conic-gradient(var(--primary-glow) var(--progress, 100%), rgba(255,255,255,0.1) 0);            display: flex;            justify-content: center;            align-items: center;            margin-top: 25px;            position: relative;        }        .timer-ring::after {            content: '';            position: absolute;            width: 55px;            height: 55px;            background: var(--bg-dark);            border-radius: 50%;        }        .timer-text {            position: relative;            z-index: 2;            font-weight: 900;            font-size: 1.3rem;        }        .ai-reply-popup {            position: absolute;            top: 20%;            left: 50%;            transform: translateX(-50%) translateY(-20px);            background: rgba(15, 23, 42, 0.9);            border: 1px solid var(--primary-glow);            border-radius: 15px;            padding: 15px 25px;            display: none;            z-index: 2000;            box-shadow: 0 10px 30px rgba(0,0,0,0.5);        }        .control-panel {            position: fixed;            right: 20px;            bottom: 20px;            width: 320px;            background: rgba(0,0,0,0.85);            padding: 20px;            border-radius: 15px;            border: 1px solid var(--glass-border);            z-index: 1000;        }        .trigger-btn {            width: 100%;            padding: 12px;            margin: 5px 0;            background: var(--primary-glow);            color: black;            border: none;            border-radius: 8px;            font-weight: 900;            cursor: pointer;            transition: transform 0.2s;        }        .trigger-btn:active { transform: scale(0.95); }        #answerSection {            margin-top: 20px;            border-top: 1px solid rgba(255,255,255,0.1);            padding-top: 15px;            animation: fadeIn 0.5s;        }        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }    </style></head><body>    <div class="tiktok-container" id="tiktokTheme">        <div class="bg-orb orb-1"></div>        <div class="bg-orb orb-2"></div>        <div class="app-content">            <header class="main-header">                <h1>الكبسولة الصحية</h1>                <div id="aiStatus" style="font-size: 0.8rem; color: var(--text-muted); text-align: center;">                    <i class="fa-solid fa-circle-check"></i> النظام جاهز                </div>            </header>            <div class="timer-ring" id="timerRing">                <span class="timer-text" id="timerText">10</span>            </div>            <div class="capsule-wrapper" id="medicineCapsule">                <div class="capsule-top"></div>                <div class="capsule-bottom"></div>                <div class="capsule-core" id="capsuleCore">                    <div id="contentIcon" style="font-size: 3rem; color: var(--primary-glow); margin-bottom: 15px;">                        <i class="fa-solid fa-heart-pulse"></i>                    </div>                    <h2 id="contentTitle" style="font-size: 1.5rem; margin-bottom: 10px;">تحميل...</h2>                    <p id="contentText" style="font-size: 1rem; line-height: 1.5; color: #eee;"></p>                                        <div id="answerSection" style="display:none;">                        <span style="background: var(--secondary-glow); padding: 2px 12px; border-radius: 20px; font-size: 0.8rem; font-weight: bold;">الحقيقة الصحية</span>                        <p id="contentAnswer" style="font-weight: 900; font-size: 1.2rem; margin-top: 12px; text-shadow: 0 0 10px rgba(255,255,255,0.2);"></p>                    </div>                </div>            </div>        </div>        <div class="ai-reply-popup" id="aiReplyPopup">            <div class="ai-reply-content">                <span id="aiReplyHeader" style="font-size: 0.75rem; color: var(--primary-glow); font-weight: bold; display: block; margin-bottom: 5px;"></span>                <p id="aiReplyText" style="font-size: 0.95rem; color: #fff;"></p>            </div>        </div>    </div>    <div class="control-panel">        <h3 style="margin-bottom: 15px; color: var(--primary-glow); border-bottom: 1px solid #333; padding-bottom: 10px;">لوحة المضيف</h3>        <input type="text" id="topicInput" placeholder="توجيه المحتوى (مثلاً: دهون الكبد)" style="width: 100%; padding: 10px; margin-bottom: 10px; background: #111; color: white; border: 1px solid #444; border-radius: 8px;">        <button class="trigger-btn" onclick="manualTrigger('info')">كبسولة معلومة</button>        <button class="trigger-btn" onclick="manualTrigger('question')">كبسولة سؤال</button>        <div id="bridgeStatus" style="font-size: 0.75rem; margin-top: 15px; color: #ff4444; text-align: center; background: rgba(255,0,0,0.1); padding: 5px; border-radius: 5px;">            <i class="fa-solid fa-link-slash"></i> الجسر غير متصل        </div>    </div>    <script>        // --- قاعدة البيانات الاحتياطية (مدمجة) ---        const massiveBackupDatabase = [            { type: "info", title: "الكبد والسكريات", text: "هل تعلم أن الإفراط في السكر يحول الكبد لمصنع دهون؟", answer: "التقليل من السكر يساهم في تنظيف الكبد وتجديده." },            { type: "question", title: "سؤال للبث", text: "ما المشروب الطبيعي الذي يساعد في ضبط سكر الدم؟", answer: "خل التفاح العضوي المخفف بالماء قبل الوجبات." },            { type: "diagnosis", title: "مقاومة الأنسولين", text: "الزوائد الجلدية في الرقبة هي جرس إنذار لارتفاع الأنسولين!", answer: "تقليل النشويات يحسن حساسية الخلايا للأنسولين تدريجياً." },            { type: "remedy", title: "شوربة الشفاء", text: "ما هو المكون الذي يرمم الأمعاء ويدعم المفاصل؟", answer: "مرق العظام الطبيعي الغني بالكولاجين والمعادن." },            { type: "mistake", title: "خطأ الفطور", text: "بدء اليوم بالحلويات أو الخبز الأبيض يرهق طاقتك!", answer: "الفطور البروتيني مع دهون صحية يثبت طاقتك لساعات." },            { type: "fix", title: "سر الشبع", text: "هل تريد شبعاً يدوم طويلاً دون رغبة في السكريات؟", answer: "ركز على الدهون الصحية كالأفوكادو وزيت الزيتون والبروتين." }        ];        const apiKey = ""; // تم تغيير الاسم ليتوافق مع تعليمات البيئة        const MODEL = "gemini-2.5-flash-preview-09-2025";        let contentQueue = [];        let activeItem = null;        let timerInterval = null;        let isFetching = false;        function updateTimerUI(val, max) {            const ring = document.getElementById('timerRing');            const text = document.getElementById('timerText');            if (text) text.innerText = Math.max(0, val);            const percent = (val / max) * 100;            if (ring) ring.style.setProperty('--progress', percent + '%');        }        function typeWriter(element, text) {            if (!element) return;            element.innerHTML = "";            let i = 0;            const speed = 40;            function type() {                if (i < text.length) {                    element.innerHTML += text.charAt(i);                    i++;                    setTimeout(type, speed);                }            }            type();        }        function startCycle() {            const capsule = document.getElementById('medicineCapsule');            if (capsule) capsule.classList.remove('open');            const answerSec = document.getElementById('answerSection');            if (answerSec) answerSec.style.display = 'none';                        prepareNextContent();            let count = 10;            updateTimerUI(count, 10);                        timerInterval = setInterval(() => {                count--;                updateTimerUI(count, 10);                if (count <= 0) {                    clearInterval(timerInterval);                    openCapsule();                }            }, 1000);        }        function openCapsule() {            if (!activeItem) return startCycle();                        const capsule = document.getElementById('medicineCapsule');            if (capsule) capsule.classList.add('open');                        typeWriter(document.getElementById('contentTitle'), activeItem.title);            typeWriter(document.getElementById('contentText'), activeItem.text);            let hasAnswer = !!activeItem.answer;            let duration = hasAnswer ? 25 : 12;            let count = duration;            updateTimerUI(count, duration);            timerInterval = setInterval(() => {                count--;                updateTimerUI(count, duration);                if (hasAnswer && count === 12) {                    const section = document.getElementById('answerSection');                    const ansText = document.getElementById('contentAnswer');                    if (section) section.style.display = 'block';                    if (ansText) typeWriter(ansText, activeItem.answer);                }                if (count <= 0) {                    clearInterval(timerInterval);                    startCycle();                }            }, 1000);        }        function prepareNextContent() {            if (contentQueue.length > 0) {                activeItem = contentQueue.shift();            } else {                activeItem = massiveBackupDatabase[Math.floor(Math.random() * massiveBackupDatabase.length)];                if (!isFetching) fetchAIContent();             }        }        // دالة جلب البيانات مع نظام التكرار الأسي (Exponential Backoff)        async function fetchAIContent() {            if (isFetching) return;            isFetching = true;            const topicInput = document.getElementById('topicInput');            const userTopic = (topicInput && topicInput.value) || "صحة الكبد وسكر الدم";            const promptText = `                أنت خبير تثقيف صحي غذائي يظهر في بث مباشر، وتقدّم محتوى عربي قوي، جذاب، متنوع، ومناسب لزيادة التفاعل والمتابعة، مع الالتزام الصارم بأن المحتوى تثقيفي فقط وليس تشخيصاً أو علاجاً فردياً.                🎯 الهدف: أنشئ 4 كبسولات محتوى حول: ${userTopic} ومواضيع (توازن سكر الدم، تقليل السكر، البروتين، الدهون الطبيعية).                ممنوع تماماً: ذكر "كيتو"، ادعاء علاج مضمون، تشخيص فردي، أو وعود قطعية.                🧠 الأسلوب: صادم منضبط، علمي مبسط، تفاعلي، قصير جداً.                🍳 المبادئ: تقليل السكر والدقيق، التركيز على الطعام الحقيقي (بيض، لحم، سمك، دجاج، كبدة، دهون طبيعية).                📌 الأنواع: أنشئ 4 عناصر فقط من (diagnosis, mistake, fix, shocking, challenge, remedy). يجب وجود عنصر واحد على الأقل من (fix أو remedy).                📏 القواعد: title: 2-4 كلمات، text: 8-12 كلمات، answer: 6-12 كلمات.                📦 أرجع JSON فقط بهذا الشكل: {"items": [{"type": "...", "title": "...", "text": "...", "answer": "..."}]}            `;            const url = `https://generativelanguage.googleapis.com/v1beta/models/${MODEL}:generateContent?key=${apiKey}`;            const delays = [1000, 2000, 4000, 8000, 16000];            for (let i = 0; i <= delays.length; i++) {                try {                    const response = await fetch(url, {                        method: "POST",                        headers: { "Content-Type": "application/json" },                        body: JSON.stringify({                             contents: [{ parts: [{ text: promptText }] }],                            generationConfig: { responseMimeType: "application/json", temperature: 0.7 }                        })                    });                    if (!response.ok) {                        const errBody = await response.text();                        throw new Error(`API Error ${response.status}: ${errBody}`);                    }                    const data = await response.json();                    if (data?.candidates?.[0]?.content?.parts?.[0]?.text) {                        const result = JSON.parse(data.candidates[0].content.parts[0].text.trim());                        if (result.items && Array.isArray(result.items)) {                            contentQueue.push(...result.items);                            document.getElementById('aiStatus').innerHTML = '<i class="fa-solid fa-robot"></i> الذكاء الاصطناعي: متصل ومستعد';                            isFetching = false;                            return;                        }                    }                    throw new Error("Invalid structure from API response");                } catch (e) {                    if (i === delays.length) {                        console.error("AI Fetch Error after all retries:", e);                        document.getElementById('aiStatus').innerHTML = '<i class="fa-solid fa-database"></i> وضع الذاكرة الاحتياطية نشط';                        isFetching = false;                        return;                    }                    await new Promise(resolve => setTimeout(resolve, delays[i]));                }            }        }        function initBridge() {            const socket = new WebSocket('ws://localhost:8081');            const status = document.getElementById('bridgeStatus');            socket.onopen = () => {                if (status) {                    status.innerHTML = '<i class="fa-solid fa-link"></i> الجسر: متصل بنجاح ✅';                    status.style.color = "#00ffcc";                    status.style.background = "rgba(0,255,204,0.1)";                }            };            socket.onmessage = (event) => {                try {                    const data = JSON.parse(event.data);                    if (data.type === 'comment') {                        triggerAIReply(data.nickname, data.comment);                    }                } catch(e) {}            };            socket.onclose = () => {                if (status) {                    status.innerHTML = '<i class="fa-solid fa-link-slash"></i> الجسر مقطوع (شغّل Node.js)';                    status.style.color = "#ff4444";                    status.style.background = "rgba(255,0,0,0.1)";                }                setTimeout(initBridge, 5000);            };        }        function triggerAIReply(user, msg) {            const popup = document.getElementById('aiReplyPopup');            const header = document.getElementById('aiReplyHeader');            const text = document.getElementById('aiReplyText');            if (popup && header && text) {                header.innerText = "رد آلي لـ " + user;                text.innerText = "أهلاً بك، جاري تحليل سؤالك: (" + msg.substring(0, 15) + "...)";                popup.style.display = "block";                setTimeout(() => popup.style.display = "none", 6000);            }        }        function manualTrigger(type) {            const item = massiveBackupDatabase.find(i => i.type === type) || massiveBackupDatabase[0];            activeItem = { ...item };            clearInterval(timerInterval);            openCapsule();        }        window.onload = () => {            initBridge();            startCycle();        };    </script></body></html>